services:
  - type: web
    name: terry-music
    runtime: docker
    plan: free
    region: singapore
    healthCheckPath: /api/health
    envVars:
      - key: MINIMAX_API_TOKEN
        sync: false
